services:
  redis:
    image: redis:alpine3.23
    restart: unless-stopped
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - sockets-network
    volumes:
      - sockets-redis-data:/data
